{{- if .Values.mongo.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
type: Opaque
stringData:
  mongo-user: {{ .Values.mongo.rootUser | quote }}
  mongo-password: {{ .Values.mongo.rootPassword | quote }}
{{- end }}
